# DATABASE_URL in standard Postgres format. Recommended to use a pooled/serverless provider (e.g., Neon or Vercel Postgres).
# Example (Neon, pooled): postgres://USER:PASSWORD@HOST/dbname?sslmode=require
DATABASE_URL=postgres://user:password@host:5432/dbname?sslmode=require

# If your provider requires SSL flags explicitly, keep sslmode=require in the URL.
